#include "monty.h"

/**
 * nop - it does nothing
 * @head: top element of stack
 * @count: line_number
 * Return: Nothing
 */
void nop(stack_t **head, unsigned int counter)
{
  (void) count;
  (void) head;
}
