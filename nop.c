#include "monty.h"

/**
 * m_nop - it does nothing
 * @head: top element of stack
 * @count: line_number
 * Return: Nothing
 */
void m_nop(stack_t **head, unsigned int count)
{
  (void) count;
  (void) head;
}
