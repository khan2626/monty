#include "monty.h"

/**
 * m_stack - it prints top element of stack
 * @top: pointer to 1st element
 * @count: line number
 * Return: Nothing
 */

void m_stack(stack_t **top, unsigned int count)
{
  (void)top;
  (void)count;
  build.l = 0;
}
